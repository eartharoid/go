# eartharoid-go

A serverless URL shortener.

Uses [Fauna](https://fauna.com/) for storage and Vercel serverless functions.

## API endpoints

- POST `/api/create`
- GET `/api/get`
- GET `/api/list`
- GET `/api/redirect`
